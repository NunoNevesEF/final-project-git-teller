spring.application.name=gitteller

#Environment Variables Files
spring.config.import=optional:file:./env.properties,optional:file:./backend/env.properties
spring.jpa.hibernate.ddl-auto=update

app.repository.mode=jpa

#spring.autoconfigure.exclude=\
#org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration,\
#org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration

#logging.level.com.zaxxer.hikari=DEBUG
#logging.level.org.springframework.jdbc=DEBUG

#Google OAuth
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}


server.servlet.session.persistent=false


# Github OAuth
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=read:user,user:email,repo
spring.security.oauth2.client.provider.github.authorization-uri=https://github.com/login/oauth/authorize
spring.security.oauth2.client.provider.github.token-uri=https://github.com/login/oauth/access_token
spring.security.oauth2.client.provider.github.user-info-uri=https://api.github.com/user
spring.security.oauth2.client.provider.github.user-name-attribute=login


app.github.app-id=${GITHUB_APP_ID}
app.github.private-key-file=${GITHUB_APP_PRIVATE_KEY_FILE}
app.github.webhook-secret=${GITHUB_APP_WEBHOOK_SECRET}
app.github.app-slug=${GITHUB_APP_SLUG}
app.github.install-url=${GITHUB_APP_INSTALL_URL}
app.github.token-cache-minutes=55


# JWT Configuration (3min access token expiration; 1d refresh token expiration)
app.jwt.expiration=180000
app.jwt.refresh-expiration=86400000




spring.datasource.url=jdbc:postgresql://localhost:5433/gitteller
spring.datasource.username=gitteller
spring.datasource.password=gitteller
spring.datasource.driver-class-name=org.postgresql.Driver